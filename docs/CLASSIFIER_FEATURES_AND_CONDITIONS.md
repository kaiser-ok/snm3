# Classifier 特徵與條件分析

## 概述

AnomalyClassifier 使用**基於規則的閾值判斷**，支援雙向檢測（Src 和 Dst 視角）。

**分類順序：** 按優先級依次判斷，命中第一個符合條件的類別即返回。

---

## 📊 使用的特徵清單

### 基礎統計特徵
| 特徵名稱 | 說明 | 單位 |
|---------|------|------|
| `flow_count` | 連線數量 | 次 |
| `unique_dsts` | 唯一目的地數量 | 個 |
| `unique_dst_ports` | 唯一目的埠數量 | 個 |
| `unique_src_ports` | 唯一來源埠數量 | 個 |
| `total_bytes` | 總流量 | bytes |
| `avg_bytes` | 平均封包大小 | bytes |
| `dst_diversity` | 目的地分散度 | 0.0-1.0 |
| `dst_port_diversity` | 目的埠分散度 | 0.0-1.0 |

### 進階特徵
| 特徵名稱 | 說明 | 單位 |
|---------|------|------|
| `flow_rate` | 連線速率 | 連線數/秒 |
| `byte_rate` | 傳輸速率 | bytes/秒 |
| `common_ports_ratio` | 常見埠比例 | 0.0-1.0 |

### 二值特徵
| 特徵名稱 | 說明 | 值 |
|---------|------|---|
| `is_high_connection` | 是否高連線數 | 0/1 |
| `is_scanning_pattern` | 是否掃描模式 | 0/1 |
| `is_small_packet` | 是否小封包 | 0/1 |
| `is_large_flow` | 是否大流量 | 0/1 |
| `is_likely_server_response` | 是否可能為服務器回應 | 0/1 |

### Dst 視角特徵
| 特徵名稱 | 說明 | 單位 |
|---------|------|------|
| `unique_srcs` | 唯一來源 IP 數量 | 個 |
| `flows_per_src` | 每個來源的平均連線數 | 次 |
| `bytes_per_src` | 每個來源的平均流量 | bytes |

---

## 🎯 Src 視角威脅分類

### 1. PORT_SCAN (埠掃描)

**代碼位置：** `nad/ml/anomaly_classifier.py:430-444`

**判斷條件：**
```python
return (
    unique_dst_ports > 100 and        # 掃描超過 100 個埠
    avg_bytes < 5000 and              # 平均封包 < 5KB
    dst_port_diversity > 0.5          # 埠分散度 > 0.5
)
```

**使用特徵：**
- ✅ `unique_dst_ports`
- ✅ `avg_bytes`
- ✅ `dst_port_diversity`

**威脅等級：** HIGH (P0)

**置信度計算：** `nad/ml/anomaly_classifier.py:566-592`
- 基礎置信度：0.6
- 埠數 > 1000：+0.2
- 埠數 > 500：+0.1
- 分散度 > 0.7：+0.15
- 分散度 > 0.6：+0.08
- 封包 < 2000：+0.1
- 封包 < 3000：+0.05

---

### 2. NETWORK_SCAN (網路掃描)

**代碼位置：** `nad/ml/anomaly_classifier.py:446-463`

**判斷條件：**
```python
return (
    unique_dsts > 50 and              # 掃描超過 50 個主機
    dst_diversity > 0.3 and           # 目的地分散度 > 0.3
    flow_count > 1000 and             # 連線數 > 1000
    avg_bytes < 50000                 # 平均流量 < 50KB
)
```

**使用特徵：**
- ✅ `unique_dsts`
- ✅ `dst_diversity`
- ✅ `flow_count`
- ✅ `avg_bytes`

**威脅等級：** HIGH (P0)

**置信度計算：** `nad/ml/anomaly_classifier.py:594-611`
- 基礎置信度：0.6
- 主機數 > 100：+0.2
- 主機數 > 70：+0.1
- 分散度 > 0.5：+0.15
- 分散度 > 0.4：+0.08

---

### 3. DNS_TUNNELING (DNS 隧道)

**代碼位置：** `nad/ml/anomaly_classifier.py:465-482`

**判斷條件：**
```python
return (
    flow_count > 1000 and             # 連線數 > 1000
    unique_dst_ports <= 2 and         # 通常只用 port 53
    avg_bytes < 1000 and              # 平均封包 < 1KB
    unique_dsts <= 5                  # 目的地 <= 5
)
```

**使用特徵：**
- ✅ `flow_count`
- ✅ `unique_dst_ports`
- ✅ `avg_bytes`
- ✅ `unique_dsts`

**威脅等級：** HIGH (P0)

**置信度計算：** `nad/ml/anomaly_classifier.py:613-628`
- 基礎置信度：0.7
- 只用 DNS 埠：+0.2
- 連線數 > 5000：+0.1

---

### 4. DDOS (DDoS 攻擊)

**代碼位置：** `nad/ml/anomaly_classifier.py:484-499`

**判斷條件：**
```python
return (
    (flow_count > 10000 or flow_rate > 30) and  # 極高連線數或速率
    avg_bytes < 500 and                          # 極小封包 < 500 bytes
    unique_dsts < 20                             # 目的地 < 20
)
```

**使用特徵：**
- ✅ `flow_count`
- ✅ `flow_rate`
- ✅ `avg_bytes`
- ✅ `unique_dsts`

**威脅等級：** CRITICAL (P0)

**置信度計算：** `nad/ml/anomaly_classifier.py:630-646`
- 基礎置信度：0.7
- 連線數 > 50000 或速率 > 100：+0.2
- 連線數 > 20000 或速率 > 50：+0.1
- 封包 < 300：+0.1

---

### 5. DATA_EXFILTRATION (數據外洩)

**代碼位置：** `nad/ml/anomaly_classifier.py:501-521`

**判斷條件：**
```python
return (
    (total_bytes > 1e9 or byte_rate > 3e6) and  # > 1GB 或 > 3MB/s
    unique_dsts <= 5 and                         # 目的地 <= 5
    dst_diversity < 0.1 and                      # 目的地集中度 < 0.1
    has_external                                 # 有外部 IP
)
```

**使用特徵：**
- ✅ `total_bytes`
- ✅ `byte_rate`
- ✅ `unique_dsts`
- ✅ `dst_diversity`
- ✅ `dst_ips`（上下文）

**威脅等級：** CRITICAL (P0)

**置信度計算：** `nad/ml/anomaly_classifier.py:648-672`
- 基礎置信度：0.7
- 流量 > 10GB 或速率 > 30MB/s：+0.15
- 流量 > 5GB 或速率 > 15MB/s：+0.1
- 目的地 = 1：+0.1
- 目的地 <= 3：+0.05
- 分散度 < 0.05：+0.05

---

### 6. C2_COMMUNICATION (C&C 通訊)

**代碼位置：** `nad/ml/anomaly_classifier.py:523-538`

**判斷條件：**
```python
return (
    unique_dsts == 1 and              # 單一目的地
    100 < flow_count < 1000 and       # 中等連線數
    1000 < avg_bytes < 100000         # 中等流量
)
```

**使用特徵：**
- ✅ `unique_dsts`
- ✅ `flow_count`
- ✅ `avg_bytes`

**威脅等級：** CRITICAL (P0)

**置信度計算：** `nad/ml/anomaly_classifier.py:674-685`
- 基礎置信度：0.6
- 單一目的地：+0.2
- 最大置信度限制：0.85（需要時間序列分析才能更準確）

---

### 7. NORMAL_HIGH_TRAFFIC (正常高流量)

**代碼位置：** `nad/ml/anomaly_classifier.py:540-562`

**判斷條件：**
```python
return (
    total_bytes > 1e9 and                        # 大流量 > 1GB
    (all_internal or                             # 全是內網 IP
     is_likely_server == 1 or                    # 或服務器回應
     is_backup_time) and                         # 或備份時間
    10 < unique_dsts < 100                       # 目的地數量合理
)
```

**使用特徵：**
- ✅ `total_bytes`
- ✅ `unique_dsts`
- ✅ `is_likely_server_response`
- ✅ `dst_ips`（上下文）
- ✅ `timestamp`（上下文）

**威脅等級：** LOW (P3)

**置信度計算：** `nad/ml/anomaly_classifier.py:687-706`
- 基礎置信度：0.5
- 是服務器回應：+0.3
- 全是內部 IP：+0.2
- 在備份時間（1-5點）：+0.1

---

## 🎯 Dst 視角威脅分類

### 1. DDOS_TARGET (DDoS 攻擊目標)

**代碼位置：** `nad/ml/anomaly_classifier.py:873-887`

**判斷條件：**
```python
return (
    unique_srcs > 100 and             # 大量不同來源 > 100
    flow_count > 1000 and             # 極高連線數 > 1000
    avg_bytes < 500                   # 小封包 < 500 bytes (SYN flood)
)
```

**使用特徵：**
- ✅ `unique_srcs`
- ✅ `flow_count`
- ✅ `avg_bytes`

**威脅等級：** CRITICAL (P0)

**置信度：** 0.95

---

### 2. SCAN_TARGET (掃描目標)

**代碼位置：** `nad/ml/anomaly_classifier.py:889-903`

**判斷條件：**
```python
return (
    unique_src_ports > 100 and        # 大量來源埠 > 100
    unique_dst_ports > 50 and         # 多個目標埠被探測 > 50
    avg_bytes < 2000                  # 小封包（探測性質）
)
```

**使用特徵：**
- ✅ `unique_src_ports`
- ✅ `unique_dst_ports`
- ✅ `avg_bytes`

**威脅等級：** HIGH (P0)

**置信度：** 0.90

---

### 3. DATA_SINK (資料外洩目標端)

**代碼位置：** `nad/ml/anomaly_classifier.py:905-921`

**判斷條件：**
```python
return (
    unique_srcs > 10 and              # 多個內部來源 > 10
    total_bytes > 100e6 and           # 大流量 > 100MB
    avg_bytes > 10000 and             # 大封包（傳輸數據）
    not self._is_internal_ip(dst_ip)  # 目標是外部 IP
)
```

**使用特徵：**
- ✅ `unique_srcs`
- ✅ `total_bytes`
- ✅ `avg_bytes`
- ✅ `dst_ip`（上下文）

**威脅等級：** CRITICAL (P0)

**置信度：** 0.85

---

### 4. MALWARE_DISTRIBUTION (惡意軟體分發)

**代碼位置：** `nad/ml/anomaly_classifier.py:923-940`

**判斷條件：**
```python
return (
    unique_srcs > 5 and               # 多個內部來源下載 > 5
    total_bytes > 50e6 and            # 大流量入站 > 50MB
    flows_per_src < 10 and            # 每來源連線少（下載後斷開）
    not self._is_internal_ip(dst_ip)  # 目標是外部 IP
)
```

**使用特徵：**
- ✅ `unique_srcs`
- ✅ `total_bytes`
- ✅ `flows_per_src`
- ✅ `dst_ip`（上下文）

**威脅等級：** CRITICAL (P0)

**置信度：** 0.80

---

### 5. POPULAR_SERVER (熱門服務器)

**代碼位置：** `nad/ml/anomaly_classifier.py:942-956`

**判斷條件：**
```python
return (
    unique_srcs > 20 and              # 大量內部來源訪問 > 20
    500 < avg_bytes < 50000 and       # 正常封包大小
    self._is_internal_ip(dst_ip)      # 目標是內部 IP
)
```

**使用特徵：**
- ✅ `unique_srcs`
- ✅ `avg_bytes`
- ✅ `dst_ip`（上下文）

**威脅等級：** LOW (P3)

**置信度：** 0.70

---

## 📌 分類優先順序

### Src 視角（按優先級）
1. PORT_SCAN (埠掃描)
2. NETWORK_SCAN (網路掃描)
3. DNS_TUNNELING (DNS 隧道)
4. DDOS (DDoS 攻擊)
5. DATA_EXFILTRATION (數據外洩)
6. C2_COMMUNICATION (C&C 通訊)
7. NORMAL_HIGH_TRAFFIC (正常高流量)
8. UNKNOWN (未知異常) - 預設

### Dst 視角（按優先級）
1. DDOS_TARGET (DDoS 攻擊目標)
2. SCAN_TARGET (掃描目標)
3. DATA_SINK (資料外洩目標端)
4. MALWARE_DISTRIBUTION (惡意軟體分發)
5. POPULAR_SERVER (熱門服務器)
6. UNKNOWN (未知異常) - 預設

---

## 🔍 特徵依賴度分析

### 高頻使用特徵 (Top 10)
1. **avg_bytes** - 8 種威脅類型使用
2. **unique_dsts** - 6 種威脅類型使用
3. **flow_count** - 5 種威脅類型使用
4. **total_bytes** - 4 種威脅類型使用
5. **unique_srcs** - 4 種威脅類型使用（Dst 視角）
6. **dst_diversity** - 3 種威脅類型使用
7. **unique_dst_ports** - 3 種威脅類型使用
8. **unique_src_ports** - 2 種威脅類型使用
9. **byte_rate** - 2 種威脅類型使用
10. **flow_rate** - 1 種威脅類型使用

### 關鍵二值特徵
- `is_likely_server_response` - 用於判斷正常高流量
- 其他二值特徵（`is_high_connection`, `is_scanning_pattern` 等）目前未直接用於分類判斷

---

## ⚠️ 已知限制

1. **閾值固定**：所有閾值都是硬編碼，未根據實際環境自適應
2. **缺少時間序列分析**：C2 通訊的週期性特徵無法有效判斷
3. **特徵未全部使用**：部分二值特徵和進階特徵未充分利用
4. **順序依賴**：分類結果依賴判斷順序，可能導致誤判

---

## 💡 優化建議

1. **動態閾值調整**
   - 根據歷史數據統計調整閾值
   - 針對不同網段設定不同閾值

2. **增加時間序列特徵**
   - 週期性檢測（C2 通訊）
   - 趨勢分析（異常持續時間）

3. **多特徵加權評分**
   - 不使用硬閾值，而是計算加權分數
   - 使用機器學習模型取代規則判斷

4. **上下文信息增強**
   - 結合設備類型
   - 結合歷史行為基線
   - 結合威脅情報

5. **混合分類模式**
   - 規則 + 機器學習
   - 先用規則快速分類，再用 ML 精確評分

---

**生成時間：** 2025-11-20
**版本：** 1.0
**基於代碼：** `nad/ml/anomaly_classifier.py`
