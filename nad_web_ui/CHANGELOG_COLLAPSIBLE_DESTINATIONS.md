# 修改記錄 - Top 通訊目的地可展開/收合功能

**日期**: 2025-11-17
**修改者**: Claude Code
**修改檔案**: `/home/kaisermac/nad_web_ui/frontend/src/views/IPAnalysis.vue`

---

## 📋 修改目的

優化「Top 通訊目的地」顯示方式，使其更節省空間，同時保留使用者查看完整列表的能力。

**問題**：
- 當目的地數量很多時（例如：100+ 筆），表格會佔用大量版面
- 後面排名的目的地流量通常很低，不需要預設顯示
- 使用者需要時應該可以展開查看完整列表

**解決方案**：
- 預設只顯示前 10 名
- 如果總數超過 10 筆，提供「展開更多」按鈕
- 點擊後可展開全部，並可再次點擊收合

---

## 🔧 修改內容

### 修改 1：新增響應式變數

**位置**: Line 784

```javascript
const showAllDestinations = ref(false)  // 控制是否展開所有目的地
```

**說明**：控制目的地列表的展開/收合狀態

---

### 修改 2：新增計算屬性

**位置**: Line 800-813

```javascript
// 計算顯示的目的地列表（根據展開狀態）
const displayedDestinations = computed(() => {
  if (!results.value?.details?.top_destinations) return []
  const destinations = results.value.details.top_destinations
  // 如果展開或總數 <= 10，顯示全部；否則只顯示前 10
  return showAllDestinations.value || destinations.length <= 10
    ? destinations
    : destinations.slice(0, 10)
})

// 計算是否需要顯示展開按鈕
const hasMoreDestinations = computed(() => {
  return results.value?.details?.top_destinations?.length > 10
})
```

**說明**：
- `displayedDestinations`: 根據展開狀態決定顯示的資料
- `hasMoreDestinations`: 判斷是否需要顯示展開按鈕

---

### 修改 3：更新標題與狀態標籤

**位置**: Line 363-375

**修改前**：
```vue
<template #header>
  <div class="card-header">
    <span>
      <el-icon><Position /></el-icon>
      Top 通訊目的地 (前 {{ results.details.top_destinations.length }} 名)
    </span>
  </div>
</template>
```

**修改後**：
```vue
<template #header>
  <div class="card-header">
    <span>
      <el-icon><Position /></el-icon>
      Top 通訊目的地
      <el-tag v-if="!showAllDestinations && hasMoreDestinations" type="info" size="small" style="margin-left: 8px;">
        顯示前 10 名，共 {{ results.details.top_destinations.length }} 名
      </el-tag>
      <el-tag v-else-if="results.details.top_destinations.length > 10" type="success" size="small" style="margin-left: 8px;">
        顯示全部 {{ results.details.top_destinations.length }} 名
      </el-tag>
    </span>
  </div>
</template>
```

**說明**：
- 標題改為固定的「Top 通訊目的地」
- 根據狀態顯示不同的標籤：
  - 收合狀態：藍色標籤「顯示前 10 名，共 X 名」
  - 展開狀態：綠色標籤「顯示全部 X 名」
  - 總數 ≤ 10：不顯示標籤

---

### 修改 4：使用計算屬性顯示資料

**位置**: Line 378-399

**修改前**：
```vue
<el-table :data="results.details.top_destinations" stripe border>
  <el-table-column type="index" label="#" width="60" />
  <!-- ... -->
</el-table>
```

**修改後**：
```vue
<el-table :data="displayedDestinations" stripe border>
  <el-table-column type="index" label="#" width="60" :index="(index) => index + 1" />
  <!-- ... -->
</el-table>
```

**說明**：
- 使用 `displayedDestinations` 計算屬性代替原始資料
- 修正索引計算，確保從 1 開始（而非 0）

---

### 修改 5：新增展開/收合按鈕

**位置**: Line 401-413

```vue
<!-- 展開/收合按鈕 -->
<div v-if="hasMoreDestinations" style="text-align: center; margin-top: 12px;">
  <el-button
    :type="showAllDestinations ? 'info' : 'primary'"
    size="small"
    @click="showAllDestinations = !showAllDestinations"
  >
    {{ showAllDestinations ? '收合列表' : `展開更多 (還有 ${results.details.top_destinations.length - 10} 筆)` }}
    <el-icon style="margin-left: 4px;">
      <component :is="showAllDestinations ? 'ArrowUp' : 'ArrowDown'" />
    </el-icon>
  </el-button>
</div>
```

**說明**：
- 只在總數 > 10 時顯示按鈕
- 收合狀態：藍色按鈕「展開更多 (還有 X 筆)」+ 向下箭頭
- 展開狀態：灰色按鈕「收合列表」+ 向上箭頭

---

### 修改 6：新增圖示匯入

**位置**: Line 781

**修改前**：
```javascript
import { Monitor, Warning, DataLine, Position, Connection, Document, ChatDotRound, Loading, Calendar, Download, Discount, Clock, InfoFilled } from '@element-plus/icons-vue'
```

**修改後**：
```javascript
import { Monitor, Warning, DataLine, Position, Connection, Document, ChatDotRound, Loading, Calendar, Download, Discount, Clock, InfoFilled, ArrowUp, ArrowDown } from '@element-plus/icons-vue'
```

**說明**：新增 `ArrowUp` 和 `ArrowDown` 圖示

---

### 修改 7：分析完成後重置展開狀態

**位置**: Line 912-913

```javascript
// 重置展開狀態（新分析時預設收合）
showAllDestinations.value = false
```

**說明**：每次執行新分析時，重置為收合狀態

---

## 📊 顯示效果

### 情況 1：總數 ≤ 10 筆
```
┌────────────────────────────────────┐
│ 📍 Top 通訊目的地                   │
├────────────────────────────────────┤
│ # │ 目的 IP        │ 流量數 │ ...  │
├───┼───────────────┼────────┼──────┤
│ 1 │ 192.168.1.100 │ 1,234  │ ...  │
│ 2 │ 192.168.1.101 │ 987    │ ...  │
│ ...                               │
│ 8 │ 192.168.1.108 │ 45     │ ...  │
└────────────────────────────────────┘
```
**行為**：直接顯示全部，不顯示展開按鈕

---

### 情況 2：總數 > 10 筆（收合狀態）
```
┌────────────────────────────────────────────────────┐
│ 📍 Top 通訊目的地                                   │
│    [顯示前 10 名，共 45 名]                         │
├────────────────────────────────────────────────────┤
│ # │ 目的 IP        │ 流量數 │ 總位元組 │ 流量佔比 │
├───┼───────────────┼────────┼─────────┼─────────┤
│ 1 │ 192.168.1.100 │ 1,234  │ 5.2 MB   │ ████ 45% │
│ 2 │ 192.168.1.101 │ 987    │ 4.1 MB   │ ███  38% │
│ 3 │ 192.168.1.102 │ 765    │ 2.8 MB   │ ██   25% │
│ ...                                               │
│ 10│ 192.168.1.109 │ 123    │ 512 KB   │ ▌    5%  │
├────────────────────────────────────────────────────┤
│           [展開更多 (還有 35 筆) ▼]                 │
└────────────────────────────────────────────────────┘
```
**行為**：只顯示前 10 筆，按鈕顯示「展開更多 (還有 35 筆)」

---

### 情況 3：總數 > 10 筆（展開狀態）
```
┌────────────────────────────────────────────────────┐
│ 📍 Top 通訊目的地                                   │
│    [顯示全部 45 名]                                 │
├────────────────────────────────────────────────────┤
│ # │ 目的 IP        │ 流量數 │ 總位元組 │ 流量佔比 │
├───┼───────────────┼────────┼─────────┼─────────┤
│ 1 │ 192.168.1.100 │ 1,234  │ 5.2 MB   │ ████ 45% │
│ 2 │ 192.168.1.101 │ 987    │ 4.1 MB   │ ███  38% │
│ ...                                               │
│ 44│ 192.168.1.143 │ 3      │ 128 B    │          │
│ 45│ 192.168.1.144 │ 1      │ 64 B     │          │
├────────────────────────────────────────────────────┤
│                 [收合列表 ▲]                       │
└────────────────────────────────────────────────────┘
```
**行為**：顯示全部 45 筆，按鈕顯示「收合列表」

---

## 💡 使用者體驗改善

### 改善前
❌ **問題**：
- 100 筆目的地全部顯示，佔用大量版面
- 需要捲動很長距離才能看到下一個卡片
- 後面 90% 的目的地流量都很低，沒必要預設顯示

### 改善後
✅ **優點**：
1. **節省空間**：預設只顯示前 10 名，大幅減少版面佔用
2. **保留彈性**：需要時可以展開查看完整列表
3. **清楚提示**：標籤明確顯示「顯示前 10 名，共 45 名」
4. **操作簡單**：一鍵展開/收合，按鈕文字動態更新
5. **視覺回饋**：
   - 收合狀態：藍色按鈕 + 向下箭頭
   - 展開狀態：灰色按鈕 + 向上箭頭

---

## 🎯 技術細節

### 展開/收合邏輯

```javascript
// 1. 計算要顯示的資料
const displayedDestinations = computed(() => {
  const destinations = results.value.details.top_destinations

  // 展開狀態 OR 總數 ≤ 10 → 全部顯示
  if (showAllDestinations.value || destinations.length <= 10) {
    return destinations
  }

  // 收合狀態 → 只顯示前 10
  return destinations.slice(0, 10)
})

// 2. 判斷是否顯示按鈕
const hasMoreDestinations = computed(() => {
  return results.value?.details?.top_destinations?.length > 10
})

// 3. 新分析時重置狀態
if (data.status === 'success') {
  results.value = data
  showAllDestinations.value = false  // 預設收合
}
```

---

## 🧪 測試建議

### 測試 1：少於 10 筆
1. 選擇一個只有少量目的地的 IP（例如：5 個目的地）
2. **驗證**：
   - ✅ 顯示全部 5 筆
   - ✅ 不顯示「展開更多」按鈕
   - ✅ 標題沒有狀態標籤

### 測試 2：超過 10 筆（收合狀態）
1. 選擇一個有很多目的地的 IP（例如：50 個目的地）
2. **驗證**：
   - ✅ 只顯示前 10 筆（排名 1-10）
   - ✅ 顯示藍色標籤「顯示前 10 名，共 50 名」
   - ✅ 顯示按鈕「展開更多 (還有 40 筆) ▼」
   - ✅ 表格排名從 1 到 10

### 測試 3：展開功能
1. 點擊「展開更多」按鈕
2. **驗證**：
   - ✅ 顯示全部 50 筆
   - ✅ 標籤變為綠色「顯示全部 50 名」
   - ✅ 按鈕變為「收合列表 ▲」
   - ✅ 按鈕顏色變為灰色
   - ✅ 表格排名從 1 到 50

### 測試 4：收合功能
1. 點擊「收合列表」按鈕
2. **驗證**：
   - ✅ 只顯示前 10 筆
   - ✅ 標籤恢復為藍色「顯示前 10 名，共 50 名」
   - ✅ 按鈕恢復為「展開更多 (還有 40 筆) ▼」
   - ✅ 頁面自動回到表格頂部（自然行為）

### 測試 5：新分析重置
1. 展開列表
2. 執行新的 IP 分析
3. **驗證**：
   - ✅ 新結果顯示時，列表自動收合
   - ✅ 只顯示前 10 筆
   - ✅ 按鈕顯示「展開更多」

---

## 📝 相關檔案

- **前端**: `/home/kaisermac/nad_web_ui/frontend/src/views/IPAnalysis.vue`
  - Line 781: 圖示匯入
  - Line 784: 新增響應式變數
  - Line 800-813: 計算屬性
  - Line 363-414: 模板修改
  - Line 912-913: 重置邏輯

---

## 🔍 未來可選改進

### 1. 記住使用者偏好
```javascript
// 使用 localStorage 記住展開狀態
const showAllDestinations = ref(
  localStorage.getItem('showAllDestinations') === 'true'
)

watch(showAllDestinations, (newValue) => {
  localStorage.setItem('showAllDestinations', newValue)
})
```

### 2. 可配置顯示筆數
```javascript
// 允許使用者自訂預設顯示筆數（10/20/30）
const defaultDisplayCount = ref(10)
```

### 3. 搜尋/過濾功能
```javascript
// 加入搜尋框，快速找到特定 IP
const searchKeyword = ref('')
const filteredDestinations = computed(() => {
  return destinations.filter(d => d.dst_ip.includes(searchKeyword.value))
})
```

---

**修改完成時間**: 2025-11-17
**狀態**: ✅ 已完成
